import React from "react";
import "./Settings.css";

const Settings = () => {
  const [difficulty, setDifficulty] = React.useState(false);

  const onClick = () => setDifficulty(true);

  return <div></div>;
};

export default Settings;
